# Chara CLI application

The CLI application for Chara Codes. Helps to sync the local project with the remote one.

## Usage
To install dependencies:

```bash
bun install
```

To run:

```bash
bun dev
```
